name: Contrast AI SmartFix Workflow

on: [push]

jobs:
  resolve_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Contrast AI SmartFix Action
        uses: Contrast-Security-OSS/contrast-ai-smartfix-action@v1.0.0